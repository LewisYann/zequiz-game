mutation CreateRound($roundType: String!) {
  createRound(roundType: $roundType) {
    errors {
      field
      message
    }
    round {
      publicId
      score
      roundType
      createdAt
      updatedAt
    }
  }
}
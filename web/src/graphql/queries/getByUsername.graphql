query GetByUsername($username: String!) {
 getByUsername(username: $username) {
    errors {
      field
      message
    }
    user {
      username
      firstname
      lastname
      email
      createdAt
      updatedAt
    }
  }
}

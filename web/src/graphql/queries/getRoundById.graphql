query GetRoundById($publicId: String!) {
  getRoundById(publicId: $publicId) {
    errors {
      field
      message
    }
    round {
      publicId
      score
      roundType
      createdAt
      updatedAt
    }
  }
}
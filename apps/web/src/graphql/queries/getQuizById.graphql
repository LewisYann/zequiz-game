query GetQuizById($getQuizByIdId: String!) {
  getQuizById(id: $getQuizByIdId) {
    id
    round {
      publicId
    }
    adult
    actorName
    originalName
    actorPicture
    movieTitle
    movieDescription
    releaseDate
    movieUrl
    quizType
    createdAt
    updatedAt
  }
}